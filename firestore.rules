rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /trades/{tradeId} {
      // Solo el dueño de la operación (por su userId) puede leerla,
      // actualizarla o borrarla.
      allow read, update, delete: if request.auth != null
        && request.auth.uid == resource.data.userId;

      // Al crear una operación nueva, el userId del documento debe
      // coincidir con el usuario autenticado que la está creando.
      allow create: if request.auth != null
        && request.auth.uid == request.resource.data.userId;
    }

    match /accounts/{accountId} {
      // Cada cuenta de trading (y sus reglas) pertenece a un único usuario.
      allow read, update, delete: if request.auth != null
        && request.auth.uid == resource.data.userId;

      allow create: if request.auth != null
        && request.auth.uid == request.resource.data.userId;
    }
  }
}
